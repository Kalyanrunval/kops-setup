---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: moniter
spec:
  selector:
    matchLabels:
      app: mobile
  template:
    metadata:
      labels:
        app: mobile
    spec:
      containers:
        - name: cont1
          image: kalyanrunval/mobilerepo:mobile
          ports:
            - containerPort: 80
